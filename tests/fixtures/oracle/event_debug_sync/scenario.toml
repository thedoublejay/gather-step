name = "event_debug_sync"
mode = "debug"

[target]
kind = "event"
qn = "order.sync"

[oracle]
expected_files = [
  # `@MessagePattern` emits `NodeKind::Event` alongside `@EventPattern` /
  # `@CustomEventPattern` (canonical-event-identity convergence), so the
  # virtual node for `order.sync` is the Event-kinded QN.
  "__event__kafka__order.sync",
]
forbidden_files = [
  "src/api_unresolved.ts",
]
max_follow_ups = 6
min_confidence = 900
required_ambiguity_codes = []
max_response_bytes = 18000
