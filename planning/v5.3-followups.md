# v5.3 Follow-up Parking Lot

Scope deferred from #52 and still valid for v5.3 follow-up work.

## Phase 1 - Correctness Bugs

- A3 - honour a trailing else as a default in if-chain enum guards (M)
- E3 - single canonical `split_top_level`; the `frontend_react.rs` copy lacks backtick handling and mis-splits template-literal args (M)
- E4 - `node_kind_label` / `edge_kind_label` fork renders AI/mirror kinds as `unknown`; needs a canonical `label()` on the enums in core (S, cross-crate)
- H4 - populate `EvidenceCitation.path` for value-mirror/guard risks (S)
- H6 - make the 256-element value-mirror array-cap truncation observable (S)

## Phase 2 - Remaining Security

- D3 - reduced git trust in `refs.rs` (attempted, reverted; needs per-operation trust)
- D5 - investigate `gix-filter` smudge/textconv exec from a malicious `.gitattributes` (M, investigation-led)
- D6 - checksum-wrap bitcode blobs with schema byte + BLAKE3, wipe-and-rebuild on mismatch (M, defense-in-depth)

## Phase 4 - Performance

- B3 - hoist `env::var("GATHER_STEP_DIAG_TS")` to `LazyLock` (S)
- B5 - collapse `path_id_bytes` triple-allocation (S)
- B6 - stop cloning full `ParseState` per augmentation pack (M)

## Phase 5 - Capability Gaps

- K11 - SIGTERM/Ctrl-C cleanup of in-progress `pr-review` temp dirs (S)
- K13a - broaden AI/LLM detection: raw OpenAI, `@anthropic-ai/sdk`, Bedrock, etc. (S)
- K4 - gateway -> backend cross-repo edge (M)
- K14 - wire the built-but-dormant Mongo/Atlas detectors (M)
- K7 - fix label-review under-indexing: 13 files / 0 routes (M, investigation-led)
