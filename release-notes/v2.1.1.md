# Gather Step v2.1.1

Patch release for setup recovery and upgrade UX.

`v2.1.1` is a focused patch on top of v2.1.0 that fixes setup-triggered indexing when stale generated state would otherwise stop the run, sharpens the operator-facing recovery messages, and clarifies the Homebrew upgrade path.

## Highlights

- Fixed `gather-step init --index` so setup-triggered indexing rebuilds generated index state from source repos instead of exiting when old `.gather-step/storage` state is stale or incompatible.
- Improved storage and schema operator messages with sentence-cased, actionable guidance. Graph schema mismatches now point to `gather-step index --auto-recover`.
- Recovery progress output now reads `Rebuilding generated index state from source repos`.
- Clarified Homebrew upgrade docs to use `brew update` followed by `brew upgrade thedoublejay/tap/gather-step`.
- Bumped the app, Cargo workspace, internal crate dependency versions, and website package metadata to `2.1.1`.

## Upgrade Notes

- Existing workspaces can keep their `gather-step.config.yaml`.
- No graph or schema changes from v2.1.0; running `gather-step init --index` after upgrading is sufficient when stale state is detected.

## Verification

- Added regression coverage for `init --index` auto-recovering stale generated state.
- Added unit coverage for graph schema mismatch operator guidance.
- Re-ran existing corrupt-graph and unsupported-metadata schema recovery tests.
- Verified manual smoke indexing with temporary generated state and direct graph/metadata/search store checks.

## Links

- Full comparison: https://github.com/thedoublejay/gather-step/compare/v2.1.0...v2.1.1
- Changelog: https://github.com/thedoublejay/gather-step/blob/main/website/src/content/docs/changelog.md#v211-2026-04-30
